<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user, class: "link" %>
	<% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, class: "link", method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
<hr>